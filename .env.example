# Validator Configuration
VALIDATOR_NAME=starknet-validator
OPERATIONAL_ADDRESS=0x1234567890abcdef1234567890abcdef12345678
VALIDATOR_PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Backup Configuration
# Set to 'false' to disable automatic backups during updates (saves storage)
# Set to 'true' to enable automatic backups during updates
ENABLE_AUTO_BACKUP=false

# Data Directory Configuration
# Set individual data directories for each service
# Leave commented to use default ./data directory for all services
# NETHERMIND_DATA_DIR=/path/to/external/ssd    # Ethereum execution client (~500GB)
# LIGHTHOUSE_DATA_DIR=/path/to/external/ssd    # Ethereum consensus client (~100GB)
# JUNO_DATA_DIR=/path/to/external/ssd          # Starknet client (~200GB)
# PROMETHEUS_DATA_DIR=./data                   # Metrics storage (~1GB)
# GRAFANA_DATA_DIR=./data                      # Dashboard config (~100MB)